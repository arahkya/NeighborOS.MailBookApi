name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: MailBookApi/dotnet restore
    - name: Build
      run: MailBookApi/dotnet build --no-restore
    - name: Test
      run: MailBookApi.Test/dotnet test --no-build --verbosity normal
